import Hero from "../../components/Hero/Hero";
import { orderPageHero } from "../../constants/heroes";

const OrderPage = () => {
  return (
    <>
      <Hero hero={orderPageHero} />
    </>
  );
};

export default OrderPage;
